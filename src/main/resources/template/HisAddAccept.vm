package ${package_name};


import com.his.session.pojo.accept.SessionAccept;
import lombok.Getter;
import lombok.Setter;


@Getter
@Setter
public class ${model}AddAccept extends SessionAccept {

    // 名称
    private String name;


    @Override
    public boolean check() {
        if (!super.check()) {
            return false;
        }
        return true;
    }
}